#include <stdio.h>

int main(void)
/*{
	char ch, sen[80];
	int i=0;
	int shift;
	
	scanf("%d\n", &shift);
	while( (ch=getchar()) != '\n')
		printf("%c", ch+shift);
	
	
	return 0;
}*/
/*
{
int ch;
ch=getchar();
printf("%d", ch);
return 0;
}		문자로 상수를 받았는데 프린트를 정수로 하니까 값이 다르게나감*/ 
{
char ch;
ch=getchar();
printf("%d", ch);
return 0;
}		문자로 상수든 문자든 받았는데 프린트를 정수로 하니까 값이 다르게 나감 
